# commands-completion-agent

## Owns
- Slash command specs and help text
- Command argument parsing
- Completion candidates for command/subcommand/key args

## Acceptance
- >= 20 usable slash commands
- Tab completion works for high-frequency paths
