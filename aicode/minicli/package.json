{
  "name": "minicli",
  "version": "0.1.0",
  "description": "MiniCLI foundation scaffold",
  "private": true,
  "type": "commonjs",
  "bin": {
    "minicli": "build/src/index.js"
  },
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "release:build": "npm run build && node build/src/release.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test:unit": "npm run build && node --test --experimental-test-isolation=none build/tests/unit/**/*.test.js",
    "test:integration": "npm run build && node --test --experimental-test-isolation=none build/tests/integration/**/*.test.js",
    "test:e2e:smoke": "npm run build && node --test --experimental-test-isolation=none build/tests/e2e/**/*.test.js",
    "test:coverage": "npm run build && node --test --experimental-test-isolation=none --experimental-test-coverage --test-coverage-lines=80 --test-coverage-functions=80 --test-coverage-branches=70 build/tests/unit/**/*.test.js build/tests/integration/**/*.test.js",
    "ci:simulate-pr": "npm run typecheck && npm run test:unit && npm run test:integration && npm run test:coverage && npm run test:e2e:smoke",
    "test": "npm run test:unit && npm run test:integration"
  },
  "devDependencies": {
    "@types/node": "^22.17.2",
    "esbuild": "^0.25.1",
    "pkg": "^5.8.1",
    "typescript": "^5.9.2"
  }
}
