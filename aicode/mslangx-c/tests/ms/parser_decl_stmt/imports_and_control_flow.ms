import core.io
from tools.util import helper as alias
var answer
fn compute(limit) {
  if (limit) print limit
  else print nil
  return answer
}
class Derived < Base {
  run() {
    return super.run
  }
}
compute(answer)